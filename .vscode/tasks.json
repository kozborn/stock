{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile stock-analyze",
      "type": "shell",
      "command": "g++ -std=c++11 main.cpp -o main.out  GUI.o Graph.o Simple_window.o Window.o `fltk-config --ldflags`",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}

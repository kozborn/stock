{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile stock-analyze",
      "type": "shell",
      "command": "g++ -g main.cpp `fltk-config --ldflags` -o main.out",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
